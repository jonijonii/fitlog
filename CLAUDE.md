# 영양·운동 통합 트래커 (가칭: FitLog)

## 프로젝트 개요

먹은 음식 + 보충제 + 운동을 기록하면 **개인 목표 대비 부족한 영양소와 운동을 알려주는** 단일 HTML 웹앱.

- 서버 없음. 모든 데이터는 사용자 브라우저의 localStorage에만 저장.
- 앱 코드는 **`index.html` 파일 하나**로 병합. GitHub Pages에 올려 링크로 공유.
  (오프라인·홈화면 추가를 위한 곁다리 파일은 아래 '파일 구조' 참고)
- 주 사용 환경은 **스마트폰**. PC는 부차적.
- 사용자는 앱 제작자 본인 + 친구들 (비개발자). **JSON이나 코드를 직접 수정할 일이 절대 없어야 한다.**

---

## 절대 제약 (반드시 지킬 것)

### 하지 말 것
- ❌ 외부 API 호출 (네트워크 없이도 완전히 작동해야 함)
- ❌ npm/빌드 도구가 필요한 구조 (webpack, vite 등)
- ❌ 특정 개인의 신체 정보·보충제 목록을 코드에 하드코딩
  - **입력 필드의 placeholder 예시값도 포함된다.** 단위는 필드 옆 접미사로 표시하고, placeholder는 비워 둘 것
- ❌ 사용자가 텍스트 에디터로 파일을 열어야 하는 설정 방식
- ❌ 의학적 진단·처방으로 읽힐 수 있는 문구 ("~를 치료합니다", "~병입니다")

### 할 것
- ✅ Vanilla JS + 인라인 CSS. 라이브러리 없이 구현
- ✅ 모든 설정을 UI에서 입력·수정·삭제 가능하게
- ✅ 첫 실행 시 온보딩 마법사로 프로필 수집
- ✅ **내보내기는 두 종류다. 하나로 합치지 말 것:**
  - **CSV** — 사람이 엑셀에서 보고 트레이너에게 보내는 표. 일별 요약 / 식사 상세 / 인바디 기록
  - **JSON** — 기기를 바꿀 때 그대로 복원하는 백업. 사람이 열어볼 파일이 아니다
  - CSV 로는 되돌릴 수 없다(끼니별 영양소 같은 중첩 구조가 표로는 표현되지 않는다).
    그래서 CSV 가 있어도 JSON 을 없애면 안 된다. 화면에서 용도를 나눠 설명할 것
  - ⚠️ **한글 CSV 는 앞에 BOM(`﻿`)을 붙여야 엑셀에서 안 깨진다**
- ✅ **파일은 Web Share API 로 건넨다** (`share.js`). `<a download>` 는 iOS, 특히 홈 화면에 추가한
  앱 안에서 낯선 앱 목록이 뜬다. 공유 시트를 쓰면 '파일에 저장'·메일·메신저가 나온다.
  지원 안 하는 브라우저(PC)에서는 자동으로 다운로드로 떨어진다
- ✅ **복사 버튼을 같이 둔다.** 폰에서 파일 주고받기가 성가실 때 클립보드 복사가 더 빠르다
- ✅ 차트는 SVG로 직접 그림 (외부 차트 라이브러리 금지)
- ✅ `<meta name="robots" content="noindex">` 포함

### 파일 구조
개발 중에는 파일을 나눠도 되지만, **앱 코드는 `dist/index.html` 하나로 병합**한다.
병합은 Node 스크립트(`build.js`)로 CSS/JS/데이터를 인라인 삽입하는 방식. 외부 빌드 도구 사용 금지.

목표 크기: 500KB 이하 (**실제 배포되는 파일만** 센다 — `dist/.git` 은 GitHub Pages 가 서빙하지 않는다).
`build.js` 가 빌드할 때마다 배포 합계를 출력한다.

**테스트 코드(`js/tests.js`)는 배포본에서 뺀다.** 개발용이라 사용자 폰에 실어 보낼 이유가 없다
(54KB — 번들에서 가장 큰 파일이었다). 검증은 `node tests/run-tests.js` 로 한다.

**단, 배포 묶음은 파일 하나가 아니다.** 서비스워커는 원리상 인라인할 수 없다 —
같은 출처의 별도 파일로 받아야 등록되고 `data:`/`blob:` URL 로는 등록이 거부된다.
오프라인 캐싱을 포기하지 않는 한 이건 피할 수 없다.

```
dist/
  index.html            앱 전체 (이 파일만 따로 열어도 동작한다. 오프라인 기능만 빠짐)
  sw.js                 서비스워커 — 별도 파일 필수
  manifest.webmanifest  홈 화면 추가 설정
  icons/                180(iOS) · 192 · 512(PWA maskable)
  .nojekyll             GitHub Pages 가 _ 경로를 건드리지 않게
```

**아이콘은 PNG 여야 한다.** iOS 의 `apple-touch-icon` 은 SVG 를 받지 않는다.
`tools/make-icons.js` 가 외부 라이브러리 없이 픽셀을 그려 PNG 로 인코딩한다.

**서비스워커가 앱을 못 열게 만들 수 있다 — 실제로 그랬다.** 두 규칙을 반드시 지킬 것:

- **`cache.addAll` 을 쓰지 말 것.** 전부-아니면-전무라서 파일 하나만 실패해도 통째로 거부되고,
  **캐시가 텅 빈 채로 워커가 켜진다.** `cache.add` 를 하나씩 돌려 실패한 것만 빠지게 한다.
- **페이지 요청에 빈 응답(504)을 주지 말 것.** 사파리가
  "인터넷에 연결되어 있지 않기 때문에 페이지를 열 수 없습니다" 로 막아버리고,
  앱이 안 열리니 **새 워커를 받을 기회도 없어 빠져나올 수 없다.**
  읽을 수 있는 HTML 을 200 으로 주고 그 안에 자가 복구 버튼(워커 해제 + 캐시 삭제)을 둔다.
- 비상 탈출구로 `?nosw` 를 지원한다 — 이 주소로 열면 워커가 스스로 물러난다.

**서비스워커는 보안 컨텍스트에서만 등록된다.** `http://192.168.x.x` 같은 LAN 주소나
`file://` 에서는 등록이 실패한다 — 정상이며, 앱은 그대로 동작한다.
오프라인 동작 확인은 HTTPS(GitHub Pages)에 올린 뒤에만 가능하다.

---

## 데이터 지속성 — 보장할 수 없다는 것을 전제로 설계한다

localStorage 는 영구 저장이 아니다. 브라우저가 저장 공간의 주인이고 앱에는 통제권이 없다.
서버를 두면 해결되지만 그건 이 프로젝트의 대전제를 뒤집는 일이다.
**보장 대신 3중 방어**로 간다.

**1. 영구 저장 요청** — 부팅할 때 `navigator.storage.persist()` 를 호출한다.
사파리는 대체로 거절한다. 거절해도 앱은 그대로 돌아가야 하며, 결과를
설정 → 앱 정보에 그대로 보여준다 (사용자가 자기 상태를 알 수 있게).

**2. 홈 화면 설치** — iOS 는 7일간 방문 없는 사이트의 script-writable storage 를 지우는데,
**홈 화면에 추가한 웹앱은 예외**라고 알려져 있다. 안내에서 '즐겨찾기 말고 홈 화면 추가' 를 강조한다.
안드로이드 크롬은 이 정책 자체가 없고, 설치한 PWA 에는 persist 를 대체로 승인한다.

**3. 백업 노출도를 눈에 보이게** — 이게 유일하게 확실한 대비다.
`meta.lastBackupAt` 을 기록하고 경과일을 보여준다.

- 설정 → 백업 카드: 마지막 백업 시점과 기록된 날 수를 항상 표시
- 14일 이상 지났거나 한 번도 안 했으면 경고로 승격
- 오늘의 알림에도 한 줄 띄운다 — **단, 기록이 3일치 미만이면 띄우지 않는다** (잃을 게 없으면 재촉하지 않는다)

**앱 업데이트로는 데이터가 날아가지 않는다.** localStorage 는 주소(origin)에 묶여 있고,
새 `index.html` 을 올려도 저장소는 건드리지 않는다. `migrate()` 는 변환만 하고 삭제하지 않는다.
이건 테스트로 강제한다 (실제 사용 중인 데이터를 migrate 에 통과시켜 전부 살아남는지 확인).

**데이터가 사라지는 진짜 경로는 따로 있다:**
주소 변경(저장소 이름·커스텀 도메인) / 브라우저 기록 삭제 / 장기 미사용.
**주소는 한 번 정하면 바꾸지 않는다.**

---

## 배포 뒤에 앱을 고쳐도 되는가

친구들이 쓰는 중에도 고칠 수 있어야 한다. 아래는 안전하며, 테스트로 강제한다.

| 고치는 것 | 이미 등록·기록된 것 |
|---|---|
| 프리셋 기본값 (오메가3 함량 등) | **안 바뀐다.** `fromPreset` 이 등록 시점에 값을 복사한다 |
| 음식·템플릿 영양소 | **안 바뀐다.** 끼니는 저장 시점 계산값을 통째로 들고 있다 |
| 데이터 구조 | 변환만 하고 삭제하지 않는다 (`migrate`) |

**지켜야 할 두 가지:**

1. **화면에 보이는 값은 저장된 데이터에서 뽑는다.**
   프리셋 라벨(`preset.amount`)을 그대로 쓰면 사용자가 함량을 고쳤거나 기본값이 바뀌었을 때
   **표시가 실제 계산값과 어긋난다.** 실제로 보충제 체크리스트가 이 문제로 거짓말을 하고 있었다.
   계산은 맞는데 표시가 틀린 건 데이터가 틀린 것보다 나쁘다 — 사용자가 앱을 못 믿게 된다.
2. **필드는 추가만 하고 지우지 않는다.** 지우면 마이그레이션이 필요해지고 거기서 실수가 난다.

프리셋 기본값을 바꿔도 이미 등록한 사람에게는 반영되지 않는다.
그런 변경은 **사용자에게 "직접 고쳐라" 고 알려야** 한다.

---

## UI 재렌더 규칙

화면은 상태가 바뀔 때마다 통째로 다시 그린다 (`FitLog.router.render()`).
단순해서 좋지만 네 가지를 지켜야 한다. **넷 다 실제로 버그가 나서 추가된 규칙이다.**

**1. 스크롤 위치를 지킨다.** 탭이 바뀔 때만 맨 위로 올리고,
같은 탭 안에서 다시 그릴 때는 보던 위치를 유지한다.
안 그러면 보충제를 연달아 체크할 때마다 화면이 맨 위로 튄다.

**2. 날짜가 바뀌면 다시 그린다.**
홈 화면에 추가한 앱은 껐다 켜지 않으면 계속 살아 있다. 자정을 넘겨도 다시 그리지 않으면
'오늘' 탭이 어제 기록을 그대로 붙들고 있게 된다 — 실제로 이 버그가 났다.
앱이 앞으로 나올 때마다(`visibilitychange` / `focus` / `pageshow`) 마지막으로 그린 날짜와
지금 날짜를 비교해 다르면 다시 그린다.

**3. 주간 범위 판정은 '이번 주' 를 문장에 밝힌다.**
최근 7일을 본 판정을 오늘 카드에 그냥 띄우면 방금 생긴 일처럼 읽힌다.
오늘 아무것도 안 먹은 날에도 어제 경고가 남아 있는 것처럼 보인다.

**4. 목록이 긴 화면은 전체 재렌더를 피한다.**
온보딩의 보충제 선택(36개)처럼 긴 목록에서는 누른 버튼만 갱신한다.
전체를 다시 그리면 스크롤이 초기화되고 입력 포커스도 날아간다.
칩·체크박스류는 선택 상태를 자기 안에서 갱신하도록 만든다.

---

## 모바일 요구사항

- **모바일 퍼스트.** 기준 폭 375px에서 설계하고 넓은 화면으로 확장
- **하단 탭바** 4개: 오늘 / 주간 / 인바디 / 설정
- 모든 터치 타겟 **최소 44×44px**
- 입력 필드 폰트 **16px 이상** (iOS 자동 확대 방지)
- 숫자 입력은 `inputmode="numeric"` 또는 `"decimal"`
- `viewport-fit=cover` + `env(safe-area-inset-*)` 로 노치·홈바 여백 처리
- **PWA 지원**: `manifest.webmanifest` + 서비스워커로 오프라인 캐싱 (둘 다 별도 파일 — 위 '파일 구조' 참고)
  - 홈 화면 추가 시 전체화면 실행 (iOS 는 manifest 가 아니라 `apple-mobile-web-app-capable` 메타를 본다)
  - 앱 아이콘은 **PNG** (iOS `apple-touch-icon` 은 SVG 를 받지 않는다)
- **기록은 3탭 안에 완료**: 오늘 탭 → 템플릿 선택 → 양 선택 → 저장
- 다크모드 지원 (`prefers-color-scheme`)

---

## 데이터 모델

```js
// localStorage key: "fitlog.v1"
{
  schemaVersion: 2,   // 저장 키("fitlog.v1")는 고정. 데이터 모양이 바뀌면 이 값만 올린다

  // 아래 값은 전부 설명용 예시다. 실제 사람의 수치를 쓰지 말 것 —
  // 이 문서는 저장소에 공개될 수 있고, 신체 정보는 민감 정보다.
  profile: {
    height: 170,          // cm, 필수
    age: 35,              // 필수
    sex: "female",        // "female" | "male", 필수
    weight: 65,           // kg, 필수
    skeletalMuscle: 25,   // kg, 선택 (인바디 골격근량)
    bodyFatPct: 28,       // %, 선택
    menopause: null,      // true | false | null, 선택 (여성 + 45세 이상일 때만 입력받는다)
    goals: ["gainStrength", "loseFat"],  // 복수 선택. "maintain"은 단독으로만 선택 가능
                                          // 저장 시 정해진 순서로 정렬 (gainStrength → loseFat → maintain)
    weeklyPlan: { strength: 2, cardio: 3 },
    createdAt: "2026-08-24"
  },

  // 자동 계산된 목표치 (프로필 변경 시 재계산)
  // 아래는 위 예시 프로필을 실제로 계산한 결과다 — 문서와 코드가 어긋나면 이 값으로 확인할 것
  targets: {
    calories: 1802, // 리컴프(근력+감량) = TDEE 2002 − 200
    protein: 143,   // g, 체중 65 × 2.2
    carbs: 195,     // g
    fat: 50,        // g
    fiber: 25,      // g
    micros: { calcium: 700, iron: 14, magnesium: 280, /* ... KDRI 14종 */ }
  },

  supplements: [
    {
      id: "sup_1",
      name: "오메가3",
      presetId: "omega3",        // 프리셋에서 왔으면 id, 직접 등록이면 null
      timeSlot: "lunch",          // "morning"|"lunch"|"afternoon"|"evening"|"bedtime"|"postWorkout"
      dailyDoses: 1,
      // 키·단위는 foods.js 와 동일 (아래 '영양소 키·단위 규약' 참고).
      // EPA/DHA 를 나누지 않고 omega3 합계로 넣는다 — 그래야 음식과 그냥 더할 수 있다.
      nutrients: { omega3: 1000 },
      enabled: true
    }
  ],

  dailyLogs: {
    "2026-08-24": {
      meals: [
        { id: "meal_...",
          type: "breakfast",
          sourceKind: "template",       // "template" | "food" | "custom"
          sourceId: "greek_yogurt_nuts",
          label: "그릭요거트 + 견과류",  // 표시용. 원본이 사라져도 기록은 읽힌다
          portion: 1.0,                 // 0.7 적게 / 1.0 보통 / 1.4 많이
          items: [{ food: "greek_yogurt", g: 150 }],  // portion 반영된 재료. 단품·직접등록이면 null
          nutrients: {...},             // 저장 시점 계산값
          time: "08:10",                // 선택. 야식·식사간격 판단용
          note: "드레싱 없이"            // 선택. 조리법·특이사항 메모 (트레이너 보고용)
        }
      ],
      alcohol: { drank: false, kcal: 0, note: "" },
      supplementsTaken: ["sup_1", "sup_3"],
      workouts: [
        { id: "wk_...", type: "strength", minutes: 60,
          bodyParts: ["lower","core"], intensity: null, note: "" },
        { id: "wk_...", type: "cardio", minutes: 30,
          bodyParts: [], intensity: "moderate", note: "" }
      ],
      waterMl: 1500,
      note: ""
    }
  },

  bodyLogs: [
    { date: "2026-08-21", weight: 65, skeletalMuscle: 25,
      bodyFatPct: 28, visceralRatio: 0.9, waistCm: null }
  ],

  customFoods: [
    // 직접 등록한 음식. 100g이 아니라 '한 번 먹는 양' 기준으로 저장한다
    // (비개발자에게 100g 환산을 시키면 안 된다)
    { id: "custom_...", name: "회사 샐러드", nutrients: {...}, createdAt: "2026-08-24" }
  ],

  favorites: ["template:kimchi_jjigae_rice", "food:greek_yogurt", "custom:custom_..."],

  // 마지막으로 백업 파일을 내보낸 시각. localStorage 는 영구 저장이 아니라서
  // '얼마나 오래 백업을 안 했는지' 를 사용자에게 보여주는 데 쓴다
  meta: { lastBackupAt: "2026-08-25T09:00:00.000Z" }
}
```

---

## 계산 로직

### 1. BMR (기초대사량)

체지방률이 **있으면** Katch-McArdle (더 정확):
```
LBM = weight × (1 - bodyFatPct/100)
BMR = 370 + (21.6 × LBM)
```

체지방률이 **없으면** Mifflin-St Jeor:
```
여성: BMR = (10 × weight) + (6.25 × height) - (5 × age) - 161
남성: BMR = (10 × weight) + (6.25 × height) - (5 × age) + 5
```

### 2. TDEE (총소비열량)

주간 운동 횟수(strength + cardio) 기준 활동계수:

| 주간 운동 횟수 | 계수 |
|---|---|
| 0–1회 | 1.20 |
| 2–3회 | 1.375 |
| 4–5회 | 1.45 |
| 6회 이상 | 1.55 |

```
TDEE = BMR × 활동계수
```

### 3. 목표별 칼로리·단백질

목표는 **복수 선택**이다. 선택 조합으로 규칙을 찾는다 (정렬 후 `+`로 이은 키).

| 목표 조합 | 칼로리 | 단백질 (적용값) |
|---|---|---|
| 근력강화 (gainStrength) | TDEE + 100 | 체중 × 1.8 g (범위 1.6~2.0) |
| 체중감량 (loseFat) | TDEE − 300 | 체중 × 2.0 g (범위 1.8~2.2) |
| 유지 (maintain) | TDEE | 체중 × 1.4 g (범위 1.2~1.6) |
| **근력강화 + 체중감량** (리컴프) | TDEE − 200 | 체중 × 2.2 g (범위 2.0~2.4) |

**선택 규칙:**
- 최소 1개는 반드시 선택. 마지막 남은 칩은 해제되지 않는다
- `maintain`은 배타적 — 고르면 나머지가 해제되고, 다른 목표를 고르면 `maintain`이 해제된다
- 저장 시 `gainStrength → loseFat → maintain` 순으로 정렬해 조합 키를 안정화

**리컴프 조합의 근거:** 근력강화(+100)와 체중감량(−300)은 칼로리 방향이 반대다.
둘 다 고르면 **얕은 적자 + 최대 단백질**로 처리한다 — 감량 속도를 늦추는 대신 근손실을 막는 방향.
사용자에게 이 선택의 의미를 안내 문구로 설명할 것.

**안전장치 (필수 구현):**
- 목표 칼로리가 `BMR × 1.05` 미만이면 → `BMR × 1.05`로 자동 상향하고 사용자에게 안내 메시지 표시
- 단백질 상한은 체중 × 2.5g로 캡

### 4. 나머지 매크로
```
지방 = 총칼로리 × 0.25 / 9   (g)
탄수 = (총칼로리 - 단백질×4 - 지방×9) / 4   (g)
식이섬유 = 총칼로리 / 1000 × 14   (g, 최소 20g)
```

---

## 영양소 기준 (한국인 영양소 섭취기준, KDRIs)

연령·성별 테이블로 내장한다. 아래는 **50대 여성 예시** — 실제 구현은 성별 × 연령대(19–29 / 30–49 / 50–64 / 65+) 조합 전부 필요.

| 영양소 | 권장(RNI/AI) | 상한(UL) | UL 적용 대상 |
|---|---|---|---|
| 칼슘 | 800 mg | 2,000 mg | 전체 |
| 철 | 8 mg (완경 후) / 14 mg (완경 전) | 45 mg | 전체 |
| 마그네슘 | 280 mg | 350 mg | **보충제 유래만** |
| 아연 | 8 mg | 35 mg | 전체 |
| 비타민 A | 650 µg RAE | 3,000 µg | 전체 |
| 비타민 D | 10 µg (400 IU) | 100 µg (4,000 IU) | 전체 |
| 비타민 E | 12 mg α-TE | 540 mg | **보충제 유래만** |
| 비타민 K | 65 µg | — | — |
| 비타민 C | 100 mg | 2,000 mg | 전체 |
| 비타민 B12 | 2.4 µg | — | — |
| 엽산 | 400 µg DFE | 1,000 µg | **보충제 유래만** |
| 칼륨 | 3,500 mg (AI) | — | — |
| 나트륨 | 1,500 mg (AI) | 2,300 mg **목표섭취량** | 전체 (과다 경고) |
| 오메가3 (EPA+DHA) | 300 mg (AI) | — | — |
| 식이섬유 | 20 g (AI) | — | — |

**중요:** 마그네슘·비타민E·엽산은 UL이 **보충제 유래분에만** 적용된다. 음식에서 온 양과 보충제에서 온 양을 별도로 집계할 것.

완경 여부는 프로필에서 **선택 입력**(여성 + 45세 이상일 때만 노출)으로 받고, 철 권장량 분기에 사용.

---

## 음식 데이터베이스

### 구조

**2층 구조다.** 단품 DB가 유일한 숫자 출처이고, 템플릿은 단품의 조합만 갖는다.
템플릿에 영양소를 직접 적지 않는다 — 재료에서 계산한다.

```js
// 1층: 개별 음식 DB (300개) — 100g 기준. 여기에만 실제 숫자가 있다
{
  id: "rice_cooked",
  name: "밥 (백미)",
  group: "grain",
  per100g: {
    kcal: 130, protein: 2.4, carbs: 28.1, fat: 0.3, fiber: 0.4,
    sodium: 1, calcium: 3, iron: 0.2, magnesium: 8, zinc: 0.6,
    vitaminA: 0, vitaminD: 0, vitaminE: 0, vitaminK: 0, vitaminC: 0,
    vitaminB12: 0, folate: 3, potassium: 29, omega3: 0
  }
}

// 2층: 식사 템플릿 (120개) — 조합 단위. 영양소는 재료에서 합산해 계산
{
  id: "kimchi_jjigae_rice",
  name: "김치찌개 + 공기밥",
  category: "korean",       // korean | banchan(집밥) | western | eatingout | snack
  baseServing: "1인분",
  items: [
    { food: "rice_cooked", g: 210 },
    { food: "kimchi_jjigae", g: 350 }
  ]
}
```

**이 구조를 쓰는 이유:**
- 템플릿 120개 × 영양소 19종을 손으로 채우면 대부분이 검증 불가능한 추정치가 된다
- 재료 조합이 곧 근거다 — "왜 620kcal인가"에 답할 수 있다
- 단품 값 하나를 고치면 그 재료를 쓰는 템플릿이 전부 같이 맞춰진다
- 템플릿 합계와 재료 합계가 어긋날 수 없다

**단위 규약 (per100g):**
`kcal` / `protein·carbs·fat·fiber` g / `sodium·calcium·iron·magnesium·zinc·vitaminC·potassium·omega3` mg /
`vitaminA(µg RAE)·vitaminD·vitaminK·vitaminB12·folate(µg DFE)` µg / `vitaminE` mg α-TE

키 이름은 `nutrition.js`의 `NUTRIENTS` 키와 정확히 일치시킨다.

**정합성 검사 (테스트로 강제):** `kcal ≈ protein×4 + carbs×4 + fat×9`.
오차가 크면 입력 실수다. 식이섬유·당알코올·주류 때문에 오차 허용은 두되, 상한을 넘으면 테스트가 깨지게 할 것.

### 카테고리 배분 (총 129개)

카테고리는 **음식 종류가 아니라 '어떻게 먹는 끼니인가'** 로 나눈다.
`korean`은 한 그릇로 끝나는 한식 한 끼, `banchan`은 밥에 반찬을 곁들이는 집밥 상차림이다.

| key | 화면 이름 | 개수 | 내용 |
|---|---|---|---|
| `korean` | 한식 | 45 | 김치찌개, 된장찌개, 비빔밥, 제육볶음, 불고기, 갈비탕, 삼계탕, 순두부, 냉면, 칼국수, 잡채밥 등 |
| `banchan` | **집밥** | 15 | 밥+생선구이+나물, 밥+계란말이+김 등 밥에 반찬을 곁들인 상차림 |
| `western` | 양식 | 25 | 파스타류, 샌드위치, 샐러드+단백질, 시리얼+우유, 토스트, 오트밀 |
| `eatingout` | 외식 | 34 | 치킨, 피자, 햄버거, 중식(짜장·짬뽕·탕수육), **일식**(초밥·라멘·우동·규동·가츠동·회덮밥), **베트남**(쌀국수·분짜·반미·짜조), 돈까스, 국밥, 분식 |
| `snack` | 간식 | 10 | 그릭요거트+견과류, 삶은계란, 과일, 아이스크림 등 |

`korean`에도 사 먹는 메뉴가 섞여 있다. 이건 의도한 것 —
같은 김치찌개라도 집에서 먹는지 밖에서 먹는지는 나트륨 기준(단품 DB)에서 이미 갈리고,
카테고리는 **찾기 쉬운 분류**가 목적이지 장소 분류가 아니다.

### 양 조절
`portion` 배수로: **적게 0.7 / 보통 1.0 / 많이 1.4**

**1인분(portion 1.0) 기준량 — 템플릿 작성 시 이 기준을 따를 것:**

| 항목 | 1인분 | 항목 | 1인분 |
|---|---|---|---|
| 공기밥 | 210 g | 찌개·국 (국물 포함) | 350 g |
| 면류 (삶은 것) | 200 g | 삼겹살·목살 구이 | **200 g** |
| 소·돼지 살코기 | 150 g | 닭가슴살 | 120 g |
| 생선구이 (고등어·갈치) | 120 g | 계란 1개 | 55 g |
| 나물·반찬 1접시 | 60 g | 김치 1접시 | 40 g |

삼겹살 200g은 흔한 1인분 기준이라 그대로 쓴다. 이 값을 바꾸면 관련 템플릿을 전부 다시 확인할 것.

### 개별 음식 DB (312개)
템플릿의 재료이자, 템플릿에 없는 걸 직접 조합할 때 쓰는 단품 목록.

| group | 개수 | 내용 |
|---|---|---|
| `grain` | 40 | 밥·죽·빵·면·시리얼 |
| `meat` | 35 | 소·돼지·닭·가공육 |
| `seafood` | 35 | 생선·조개·오징어·건어물 |
| `vegetable` | 50 | 채소·나물·버섯·김치 |
| `fruit` | 30 | 과일 |
| `dairy` | 25 | 우유·요거트·치즈·계란 |
| `legume_nut` | 25 | 콩·두부·견과 |
| `seasoning` | 25 | 기름·장류·소스·당류 |
| `dish` | 47 | 조리된 국·찌개·볶음, 일식 면·덮밥 고명, 베트남 음식 등 재료로 쪼개기 어려운 완성 요리 |

`dish`는 김치찌개 국물처럼 단품으로 환원하면 오차가 더 커지는 항목이다.
이 경우 100g당 값을 직접 넣되, **조리 기준을 주석으로 남길 것**.

**나트륨 기준은 '어디서 먹는 음식이냐'로 나눈다:**
- 집·구내식당에서 해먹는 음식(찌개·국·볶음밥·잡채·파스타·제육볶음 등) → **집밥 기준**
- 사 먹기만 하는 음식(치킨·피자·짜장·짬뽕·탕수육·떡볶이·국밥·냉면) → **식당 기준 유지**

한 끼에 하루 나트륨 목표섭취량(2,300mg)을 넘는 일이 흔하므로, 판정 엔진이
나트륨을 과하게 경고하지 않도록 주간 평균 기준으로 다룰 것.

### 직접 등록
이름 + 칼로리 + 단백질(필수), 나머지 선택 입력 → `customFoods`에 저장, 다음부터 검색에 노출.
**100g 기준이 아니라 '한 번 먹는 양' 기준으로 입력받는다.** 양 조절은 여기에 배수로 적용된다.

### 즐겨찾기
`favorites`에 `"<종류>:<id>"` 형태로 저장한다 (`template` / `food` / `custom`).
직접 등록한 음식을 지우면 해당 즐겨찾기도 같이 지운다.

### 데이터 출처 주석
각 항목에 대략적 근거를 주석으로 남기고, **앱 하단에 "영양성분은 일반적인 조리 기준 추정치라 실제랑 차이가 있을 수 있어" 고지**를 표시할 것.

---

## 보충제 시스템

### 프리셋 (자주 쓰는 것들의 카탈로그, 함량 수정 가능)

**아래 표는 '이 앱으로 기록할 수 있는 보충제의 전부'가 아니다.**
자주 쓰이는 것을 빠르게 고르라고 있는 목록일 뿐이고, 여기 없는 제품은
**직접 등록**으로 넣을 수 있어야 한다 (이름 + 시간대 + 하루 횟수 → 등록 후 성분 함량 추가).
이게 없으면 프리셋 목록이 곧 제작자 개인의 복용 목록이 되어 절대 제약을 어기게 된다.

목록이 길어지므로 **종류별로 묶어서** 고르게 한다:
비타민·미네랄 / 오메가·지용성 / 단백질·운동 / 수면·컨디션 / 기타

성분은 KDRI 14종 + 단백질 + 칼로리 중에서 골라 붙인다. 붙인 성분은 하루 합계·중복 판정·UL 판정에 그대로 들어간다.


| 프리셋 | 기본 함량 | 기본 시간대 |
|---|---|---|
| **비타민·미네랄** (14) | | |
| 종합비타민 | 대표 성분값 | 점심 |
| 비타민 D | 1,000 IU (25 µg) | 점심 |
| 비타민 K2 | 100 µg | 점심 |
| 아연 | 15 mg | 점심 |
| 철분 | 25 mg | 아침 공복 |
| 비타민 C | 1,000 mg | 점심 |
| 칼슘 | 500 mg | 저녁 |
| 마그네슘 (트레온산) | 원소 Mg 144 mg | 취침 전 |
| 비타민 B 컴플렉스 | 제품 기준 | 점심 |
| 엽산 | 400 µg | 점심 |
| 비타민 E | 400 IU (268 mg α-TE) | 점심 |
| 비타민 A | 700 µg RAE | 점심 |
| 멀티미네랄 | 대표 성분값 | 저녁 |
| 셀레늄 | 200 µg | 점심 |
| **오메가·지용성** (5) | | |
| 루테인 | 20 mg | 점심 |
| 오메가3 | EPA 600 / DHA 400 mg | 점심 |
| 코엔자임Q10 | 100 mg | 점심 |
| 감마리놀렌산 (달맞이꽃) | 1,000 mg | 점심 |
| 쏘팔메토 | 320 mg | 점심 |
| **단백질·운동** (4) | | |
| 유청 단백질 파우더 | 단백질 24 g / 120 kcal | 운동 후 |
| 크레아틴 모노하이드레이트 | 5 g | 운동 후 |
| BCAA | 5 g | 운동 후 |
| 아르기닌 | 3,000 mg | 운동 후 |
| **수면·컨디션** (3) | | |
| L-테아닌 | 200 mg | 취침 전 |
| 멜라토닌 | 3 mg | 취침 전 |
| 아슈와간다 | 600 mg | 취침 전 |
| **기타** (10) | | |
| TMG (베타인) | 1,000 mg | 아침 공복 |
| NMN | 300 mg | 아침 공복 |
| 프로바이오틱스 | 제품 기준 | 아침 공복 |
| 콜라겐 | 5,000 mg | 취침 전 |
| 이노시톨 | 2,000 mg × 2회 | 아침 공복 |
| 밀크씨슬 | 실리마린 130 mg | 점심 |
| 글루코사민 | 1,500 mg | 점심 |
| MSM | 1,500 mg | 점심 |
| 홍삼 | 제품 기준 | 아침 공복 |
| 은행잎 추출물 | 120 mg | 점심 |

함량은 시중 제품의 흔한 값이며, 등록할 때 사용자가 고칠 수 있어야 한다.
**이 목록에 없는 제품은 직접 등록으로 넣을 수 있어야 한다** — 그게 없으면
이 표가 곧 '먹을 수 있는 것의 전부'가 되어 절대 제약(개인 목록 하드코딩 금지)을 어기게 된다.

### 제품 라벨 숫자 ≠ 영양소로 세는 값

제품 앞면 숫자를 그대로 넣으면 과대 계상된다. 이런 프리셋은 반드시 `note` 를 갖고,
**목록에서 고르는 순간 보여준다** — 등록한 뒤 펼쳐야 보이면 이미 잘못 넣은 다음이다.

| 프리셋 | 앞면 표기 | 실제로 세는 값 |
|---|---|---|
| 오메가3 | 어유 전체 무게 (예: 1,000mg) | EPA + DHA 합계 |
| 마그네슘 | 화합물 총량 (예: 트레온산마그네슘 2,000mg) | 원소 마그네슘 (약 144mg) |
| 비타민 D·E | IU | µg / mg α-TE |

**마그네슘 주의:** 제품 표기 화합물 총량(예: 2,000mg 트레온산마그네슘)과 원소 마그네슘 함량(약 144mg)이 다르다. 입력 시 "원소 함량 기준"임을 명시할 것.

### 시간대
`morning`(아침 공복) / `lunch`(점심) / `afternoon`(오후) / `evening`(저녁) / `bedtime`(취침 전) / `postWorkout`(운동 후)

### 상호작용 룰 엔진

**충돌 경고 (같은 시간대에 배치되면 알림):**
- 철분 ↔ 칼슘 / 마그네슘 / 아연 → 흡수 경쟁. 2시간 이상 간격 권장
- 철분 ↔ 종합비타민(칼슘·아연 포함 시) → 동일

**긍정 조합 (잘 배치했을 때 칭찬 표시):**
- 철분 + 비타민C → 흡수 향상
- 비타민D + 비타민K2 → 칼슘 대사 시너지
- 지용성(D, K, A, E, 루테인, 오메가3, CoQ10) + 식사 → 흡수 향상

**시간대 권고:**
- 지용성 계열이 `morning`(공복)에 있으면 → 식후로 옮기라고 안내
- 마그네슘·L-테아닌이 아침에 있으면 → 졸음 유발 가능, 취침 전 권장

### 성분 중복 경고

종합비타민에 이미 비타민 D가 들어 있는데 비타민 D를 따로 먹는 경우처럼,
**같은 영양소를 두 개 이상에서 받고 있으면 UL을 넘지 않아도 알려준다.**
어떤 제품들에서 오는지와 합계를 같이 보여줄 것.

> "종합비타민이랑 비타민 D 둘 다 비타민 D가 들어 있어. 합치면 하루 35µg이야."

### 영양소 키·단위 규약

보충제의 `nutrients`는 **`foods.js`와 똑같은 키·단위**를 쓴다. 그래야 음식 + 보충제 합산이
그냥 더하기가 되고, UL 판정도 한 곳에서 끝난다.
오메가3는 EPA/DHA를 나누지 않고 `omega3`(mg) 합계로 넣는다 (표시용 문구에만 EPA/DHA를 적는다).
단백질 보충제는 `protein`(g) / `kcal`도 채워서 매크로 합계에 반영되게 한다.

### 상한량(UL) 경고
음식 + 보충제 합산으로 UL 초과 시 경고 배지 표시.
**단, 마그네슘·비타민E·엽산은 보충제 유래분만 UL과 비교.**

경고 문구는 정보 제공 톤으로:
> "비타민 D 섭취량이 상한섭취량(4,000 IU)을 넘었어. 제품 함량 확인해 보고, 계속 이 수준이면 전문가랑 상의해 봐."

---

## 판정 엔진

### 영양 판정 (일간 + 주간 평균)

각 영양소를 4단계로:
- 🔴 **부족** — 권장량의 70% 미만
- 🟡 **약간 부족** — 70~90%
- 🟢 **적정** — 90% 이상
- 🟠 **과다** — 아래 기준

**'과다'의 기준은 미량영양소와 매크로가 다르다:**

| | 과다 기준 | 이유 |
|---|---|---|
| 미량영양소 | **UL 초과** (나트륨은 목표섭취량 초과) | 권장량을 좀 넘는 건 문제가 아니다. 110~UL 구간을 과다로 부르면 매일 거짓 경고가 뜬다 |
| 매크로 (칼로리·단백질·탄수·지방·식이섬유) | **권장량의 110% 초과** | UL 이라는 개념이 없고 초과 자체가 문제다 |

**부족 판정과 상한 판정은 서로 다른 값을 본다.**
마그네슘·비타민E·엽산은 상한을 **보충제 유래분**으로만 따지지만,
부족한지는 **음식까지 합친 총량**으로 봐야 한다. 이 둘을 같은 값으로 처리하면
보충제를 안 먹는 사람의 마그네슘이 늘 🔴 부족으로 잡힌다.

**주간 평균은 기록이 있는 날만 나눈다.** 기록이 없는 날은 '안 먹은 날'이 아니라
'안 적은 날'이라서, 0으로 넣고 평균 내면 실제보다 훨씬 부족하게 나온다.

**주간 탭은 주간 평균**, **오늘 탭은 오늘 값**을 쓴다.
주간 평균은 '요즘 어떤가' 를 보는 것이고, 오늘 화면에서 알고 싶은 건 '지금 뭐가 모자라고 뭐가 넘쳤나' 다.
오늘 카드에 주간 평균을 쓰면 오늘 먹은 것과 숫자가 안 맞아 혼란만 준다.

**총량을 말할 때는 항상 하루 필요량 대비를 같이 말한다.**
"비타민 D 35µg" 만 던지면 그게 많은 건지 적은 건지 알 수 없다.
"하루 필요량(10µg)의 3.5배, 상한 100µg 아래" 처럼 판단할 수 있게 적을 것.
상한을 넘었으면 경고하고, 필요량만 넘었고 상한 아래면 문제 아님을 명시한다.

**보충제 배치 조언(충돌·좋은 조합·시간대 권고)은 오늘 탭이 아니라 설정 탭에 둔다.**
시간대를 고칠 수 있는 자리에 있어야 행동으로 이어진다.

우선순위 정렬: 단백질 → 식이섬유 → 🔴 부족 미량영양소 → 🟠 과다 순.

### 운동 판정 (주간)

**독려·잔소리는 넣지 않는다.** 사용자 요청: "운동에 대해 독려하는 내용은 빼줘. 필요한 정보만 받고 싶어."
계획 대비 횟수 같은 건 주간 탭에 `근력 1/3회` 라는 숫자로 이미 있다. 문장으로 또 말할 이유가 없다.

남기는 판정은 **모르면 손해 보는 사실** 두 가지뿐:

1. `goals.includes("loseFat")` && 주간 strength == 0 → **근손실 위험 경고** (최우선)
2. strength 기록일에 단백질이 목표의 80% 미만 → "**이번 주에** 근력운동 한 날 단백질이 부족했어 (N일). 근성장에 손해야"
   - '이번 주에' 를 빼면 안 된다. 최근 7일을 본 판정이라 오늘 아무것도 안 먹은 날에도 남아 있는데,
     범위를 안 밝히면 방금 생긴 경고처럼 읽힌다

**뺀 것** (다시 넣지 말 것): 남은 근력운동 횟수 안내 / 4주간 빠진 부위 지적 /
연속 휴식 리마인더 / 주간 계획 100% 달성 축하.

### 신체 변화 판정
- 주간 체중 변화율이 체중의 1% 초과 감소 → "감량 속도가 좀 빨라. 근육 손실 위험이 있어"
- 4주간 체중 변화 없음 + 근력운동 지속 → "저울은 그대로여도 체성분은 바뀌는 중일 수 있어. 허리둘레 확인해 봐"

---

## 화면 구성

### 온보딩 (첫 실행)
4단계 마법사. 각 단계 건너뛰기 불가(필수 항목만), 뒤로 가기 가능.
1. 키 / 나이 / 성별 / 체중
2. 인바디 수치 (골격근량, 체지방률) — **건너뛰기 가능**
3. 목표 선택 (복수 가능) + 주간 운동 계획
4. 보충제 등록 — 프리셋 목록에서 체크 후 시간대 지정. **건너뛰기 가능**

완료 시 계산된 목표치를 요약해서 보여주고 "시작하기".

### 탭 1: 오늘
- 상단: 칼로리·단백질 진행 링 (남은 양 표시)
- 식사 섹션: 아침/점심/저녁/간식 — 각각 `+` 버튼 → 템플릿 선택 시트
- 보충제 섹션: 시간대별 체크박스. 이미 지난 시간대인데 미체크면 연한 강조
- 운동 섹션: 근력/유산소 버튼 → 시간·부위 입력
- 하단: 오늘의 알림 카드 (부족 영양소 top 3 + 운동 알림)

### 탭 2: 주간
- 체중·허리둘레 SVG 라인 차트
- 주간 운동 달성률 (근력 2/2, 유산소 2/3 형태)
- 영양소 주간 평균 리포트 (막대 + 4단계 색상)
- UL 초과 경고 목록

### 탭 3: 인바디
- 측정 기록 입력 폼: 날짜, 체중, 골격근량, 체지방률, 복부지방률, 허리둘레
- 골격근량 ↑ / 체지방량 ↓ 이중 축 차트
- 이전 측정 대비 변화량 표시

### 탭 4: 설정
- 프로필 수정 (수정 시 목표치 재계산 + 변경 내역 안내)
- 목표 변경
- 보충제 추가/수정/삭제/일시중지
- **데이터 내보내기** (JSON 파일 다운로드)
- **데이터 불러오기** (JSON 파일 선택)
- 전체 초기화 (2단계 확인)
- 앱 정보 + 면책 고지

---

## 테스트 원칙

이 앱은 폰에서 돌고, 나는 폰에서 실행해 볼 수 없다. 그래서 **화면에서만 드러나는 버그를
코드 쪽에서 잡아내는 장치**를 쌓아 왔다. 아래는 전부 실제로 버그가 나서 생긴 것들이다.

### 검증 3단

| | 명령 | 잡는 것 |
|---|---|---|
| 단위 | `node tests/run-tests.js` | 계산·판정·저장 로직, ES5/CSS/배포 정적 검사 |
| 브라우저 | jsdom 으로 `dist/index.html` 을 띄워 실제 클릭 | 온보딩~기록~공유 전 과정 |
| 서비스워커 | 가짜 caches/fetch 환경에 `sw.js` 를 태움 | 오프라인 동작 |

### 정적 검사 (`tests/run-tests.js` 뒷부분)

- **ES5 검사** — `src/js` 전체에서 화살표 함수·템플릿 리터럴·`padStart` 등을 찾는다.
  하나만 섞여도 구형 브라우저에서 **파일 전체가 죽고**, 사용자에게는 '버튼이 안 눌린다' 로만 보인다.
  실제로 `padStart` 하나 때문에 온보딩 마지막 단계가 멈췄다.
- **CSS 검사** — 전역 `[hidden]{display:none !important}` 규칙이 있는지 확인한다.
  jsdom 은 기본 스타일 우선순위를 실제 브라우저와 다르게 계산해서 이 부류를 못 잡는다.
- **배포 검사** — `dist` 의 파일 구성, manifest 유효성, sw 캐시 버전이 앱 빌드와 같은지.

가드를 새로 만들면 **일부러 깨뜨려 보고** 실제로 실패하는지 확인할 것.
안 그러면 아무것도 안 잡는 가드를 통과했다고 착각하게 된다.

### 함정 세 가지

**1. jsdom 을 믿지 말 것.** `[hidden]` 우선순위를 실제 브라우저와 다르게 계산한다.
스모크 105건이 전부 통과하는 동안 실제 폰에서는 온보딩이 안 닫혔다.
화면에 보이느냐가 걸린 문제는 CSS 소스를 직접 검사한다.

**2. 함수를 갈아끼우지 말고 환경을 바꿀 것.**
`store.todayKey` 만 스텁하면 내부에서 그 함수를 부르는 `getDay` 는 여전히 진짜 오늘을 본다.
날짜 관련은 **시계(`Date`)를 돌려서** 앱 전체가 같은 날짜를 보게 해야 실제 상황과 같아진다.

**3. 갈아끼울 자리가 필요하면 내보낸 객체를 거치게 할 것.**
`share.deliverFile` 은 일부러 `FitLog.share.downloadFile()` 을 부른다.
내부 함수를 직접 부르면 공유/다운로드 두 갈래를 각각 확인할 방법이 없다.

### 테스트가 데이터를 지우지 않게

`FitLog.runTests()` 는 브라우저 콘솔에서도 돌릴 수 있다.
저장소를 건드리는 테스트는 **시작할 때 스냅샷을 뜨고 끝나면 `store.replace()` 로 되돌린다.**
안 그러면 사용자가 테스트를 한 번 돌렸다가 기록을 통째로 잃는다.

---

## 개발 단계

각 Phase 완료 시 동작하는 상태로 커밋하고 멈춘다. 한 번에 전부 만들지 말 것.

### 진행 상태

| Phase | 상태 | 비고 |
|---|---|---|
| 1 뼈대 | 완료 | |
| 2 음식 | 완료 | 단품 312개 / 템플릿 129개 |
| 3 보충제 | 완료 | 프리셋 36종 + 직접 등록 |
| 4 판정 & 대시보드 | 완료 | 운동 판정은 2종으로 축소 (아래 참고) |
| 5 배포 | 완료 | https://jonijonii.github.io/fitlog/ 운영 중 |
| 6 공유 | **텍스트만 완료** | PNG 카드·커스터마이징·리마인더는 미착수 |

배포 후에 사용자 피드백으로 바뀐 것들이 있다. 스펙과 코드가 어긋나 보이면
**이 문서가 최신**이다 — 바뀔 때마다 여기에 반영했다.

### Phase 1 — 뼈대
- HTML 스캐폴드 + 하단 탭바 + 라우팅
- localStorage 데이터 레이어 (읽기/쓰기/마이그레이션)
- 온보딩 마법사
- BMR/TDEE/목표치 계산 함수 + 단위 테스트용 콘솔 검증
- 데이터 내보내기/불러오기
- **검수 기준:** 프로필 넣으면 목표 칼로리·단백질이 나오고, 새로고침해도 유지된다

### Phase 2 — 음식
- 식사 템플릿 120개 + 개별 음식 300개 데이터 작성
- 검색 UI + 템플릿 선택 시트 + 양 조절
- 직접 등록 + 즐겨찾기
- 일일 매크로 합산 표시
- **검수 기준:** 하루 세 끼를 3탭씩으로 기록할 수 있고 칼로리·단백질 합계가 맞다

### Phase 3 — 보충제
- 프리셋 19종 + 등록/수정 UI
- 시간대별 체크리스트
- 음식 + 보충제 영양소 합산 (보충제 유래분 별도 집계)
- UL 경고 + 상호작용 룰 엔진
- **검수 기준:** 종합비타민 + 비타민D를 같이 등록하면 D 중복이 잡힌다

### Phase 4 — 판정 & 대시보드
- 영양 4단계 판정 + 우선순위 정렬
- 운동 판정 룰 6종
- 신체 변화 판정
- 주간 탭 + 인바디 탭 SVG 차트
- **검수 기준:** 2주치 더미 데이터를 넣으면 의미 있는 리포트가 나온다

### Phase 5 — 배포
- PWA manifest + 서비스워커 + 앱 아이콘
- 모바일 세부 조정 (safe-area, 터치 타겟, 폰트 크기)
- 다크모드
- `build.js`로 단일 `dist/index.html` 병합
- 실제 폰에서 테스트 (iOS Safari + Android Chrome)
- **인앱 브라우저 감지 + 진입 배너** (`env.js`) — 배포 후 실사용에서 추가됨
- **검수 기준:** 폰에서 홈 화면에 추가하면 전체화면으로 뜨고, 비행기 모드에서도 기록된다

#### 인앱 브라우저 감지 (`env.js`)

친구들은 링크를 카톡·인스타로 받아 그 앱의 **인앱 브라우저**로 연다.
인앱 브라우저는 localStorage 가 세션 종료 시 날아가거나 막혀서 **기록해도 사라진다.**
사용자는 이유를 모른 채 '앱이 이상하다' 고 느낀다 — 그래서 감지해 경고하고 탈출로를 준다.

- **감지**: `navigator.userAgent` 로 카카오톡(`KAKAOTALK`)·인스타그램(`Instagram`)·라인(`Line/`)·
  네이버앱(`NAVER(inapp`)·페이스북(`FB(AN|AV|_IAB)`)을 판별. 감지 함수는 UA 를 인자로 받는
  **순수 함수**라 실제 앱 UA 문자열로 테스트한다.
- **진입 배너** — 부팅 시 우선순위대로 **하나만** 띄운다:
  1. 인앱 브라우저 → 빨강 경고 "기록이 저장되지 않아요" + 탈출 버튼
     - **카톡**: `kakaotalk://web/openExternal?url=<인코딩 주소>` 스킴으로 바깥 브라우저 열기
     - **그 외**: 주소가 제각각이라 **주소 복사** 버튼 (붙여넣어 직접 열게)
  2. (인앱 아님) 저장 불가 → 빨강 경고 (사파리 시크릿 탭 등)
  3. (저장 됨) 홈 화면 미설치 → 노랑 안내 "홈 화면에 추가", **닫기 가능**
  4. 홈 화면 앱(standalone) → 배너 없음
- **standalone 판정**: `matchMedia('(display-mode: standalone)')` 또는 `navigator.standalone`
- 라인은 단어 `line` 오탐을 막으려 `Line/`(슬래시 포함)으로 감지한다.

### Phase 6 — 공유 (트레이너 보고용)
- [x] **텍스트 요약 + 클립보드 복사** (`report.js`) — 오늘 탭·주간 탭 아래 '요약 복사'
- [x] **Web Share API 연동** (`share.js`) — CSV·백업 파일 전달에 사용
- [ ] 일일/주간 요약 카드 PNG (canvas)
- [ ] 공유 항목 커스터마이징 설정
- [ ] 저녁 리마인더 알림
- **검수 기준(텍스트):** 오늘 탭 → 요약 복사 → 카톡에 붙여넣으면 표가 안 깨지고 읽힌다 ✔
- **검수 기준(이미지):** 폰에서 공유 버튼 → 카톡 선택 → 이미지 전송까지 3탭 안에 된다

---

## 공유 기능 상세 (Phase 6)

### 설계 원칙
- **이 카드가 트레이너에게 가는 유일한 보고 수단이다.** 사용자는 음식 사진을 따로 보내지 않는다. 사진이 담당하던 정보 — **양, 조리 방식, 식사 시각** — 을 카드가 텍스트로 대신 전달해야 한다.
- 따라서 끼니 항목에는 **양 표기(적게/보통/많이)와 시각, 메모를 포함**한다. 이게 빠지면 트레이너가 판단할 근거가 사라진다.
- 트레이너 관심사는 **매크로·식사 구성·식사 시각·운동 수행·체중 추세**로 한정된다.
- **미량영양소, 보충제 상세, UL 경고는 기본적으로 공유 카드에서 제외한다.** (개인 관리 영역)
- 카드는 스크롤 없이 한 화면에 읽혀야 한다.

### 두 가지 출력

**1) 이미지 카드 (PNG)**
canvas로 렌더링. 사람이 눈으로 훑는 용도.
- 일일 카드: 1080 × 1350px (세로), 카톡·밴드 썸네일에서 잘 보이는 비율
- 주간 카드: 1080 × 1500px
- 다크모드와 무관하게 항상 밝은 배경 (인쇄·캡처 대비)
- 폰트는 시스템 폰트만 사용 (외부 폰트 로드 금지)

**복사 전에 무엇이 나갈지 보여준다.** 복사만 시키면 확인 없이 붙여넣게 된다.
오늘 탭·주간 탭의 요약 카드는 복사 버튼 위에 실제 내용을 그대로 띄운다.
요약 텍스트는 공백으로 칸을 맞추므로 **고정폭 글꼴**로 보여야 붙여넣은 모습과 같다.

날짜는 **어제 / 오늘 버튼 + 날짜 칸**으로 고른다. 하루가 저녁까지 먹어야 완성되므로
실제 사용은 '다음 날 아침에 어제 것을 보내기' 가 된다. 그래서 두 버튼이 주 경로다.
며칠 전 기록은 날짜 칸으로 가고, 여러 날을 한 번에 보는 건 주간 요약이 맡는다.

**2) 텍스트 (클립보드 복사)** — 구현 완료 (`report.js`)
트레이너가 눈으로 읽거나 AI 도구에 붙여넣는 용도. 이미 계산된 수치라 사진 추정보다 정확하다.
- 마크다운 아닌 플레인 텍스트. **표 문법(`|`)을 쓰지 않는다** — 카톡에서 깨진다
- 이모지를 쓰지 않는다 (AI 파싱 방해)
- 단위 명시 (`98g`, `1,420kcal`). 천 단위 쉼표를 붙인다
- 시각이 없는 끼니는 그 자리를 공백으로 채워 **끼니 이름이 세로로 정렬**되게 한다
- 메모는 20자에서 자른다 (길면 카드가 무너진다)

**CSV 와 요약 텍스트는 받는 쪽이 다르다. 하나로 합치지 말 것:**

| 받는 쪽 | 형식 | 위치 |
|---|---|---|
| 엑셀·구글시트, **AI** | CSV (쉼표 표) | 설정 → 엑셀로 보기 |
| **사람이 카톡에서 훑어보기** | 요약 텍스트 | 오늘 탭·주간 탭 아래 |

CSV 를 카톡에 붙이면 쉼표 범벅으로 보여 읽을 수 없다. 반대로 요약 텍스트는 표 계산에 못 쓴다.

### 일일 카드 내용 (기본값)

```
{날짜} ({요일})

칼로리   {실제} / {목표}
단백질   {실제}g / {목표}g
탄수 {}g · 지방 {}g

{시각} 아침  {메뉴} ({양})
              └ {메모}
{시각} 점심  {메뉴} ({양})
{시각} 저녁  {메뉴} ({양})
       간식  {메뉴}

물    {ml}
음주  {내용}                ← 기록 있을 때만
운동  {종류} {분}분 ({부위})
체중  {kg}
```

- **양 표기는 필수.** `portion` 값을 적게/보통/많이로 변환해 항상 노출한다.
- 시각·메모는 기록이 있을 때만 표시하고, 없으면 줄을 당겨 붙인다 (빈칸 표시 금지).
- 메모는 끼니당 최대 20자까지만 카드에 노출 (넘치면 말줄임).

### 주간 카드 내용 (기본값)

```
{월} {N}주차 ({시작}–{종료})

평균 칼로리   {} / {목표}
평균 단백질   {}g / {목표}g
목표 달성      {N}일 / 7일

근력 {실제}/{계획}   유산소 {실제}/{계획}
체중 {시작} → {종료} ({변화})
허리 {시작} → {종료}       ← 기록 있을 때만

이번 주 갭
· {판정 엔진 상위 2~3개}
```

**"이번 주 갭"은 판정 엔진 결과 중 트레이너 관련 항목만 필터링**한다.
포함: 단백질/칼로리 부족·과다, 운동 미달, 감량 속도 이상, 식이섬유 부족
제외: 미량영양소, 보충제 관련, UL 경고

### 커스터마이징 (설정 탭)

공유 카드에 포함할 항목을 체크박스로 제어:
- [ ] 칼로리 · [ ] 매크로 상세 · [ ] 끼니별 메뉴 · [ ] 간식
- [ ] 식사 시각 · [ ] 끼니 메모 · [ ] 물 · [ ] 음주
- [ ] 운동 · [ ] 체중 · [ ] 허리둘레
- [ ] 미량영양소 요약 (기본 OFF)
- [ ] 보충제 체크 현황 (기본 OFF)

**양 표기는 토글 대상이 아니다. 항상 포함된다.**

기본 프리셋 2종 제공: **"트레이너용"**(위 기본값) / **"전체"**(모든 항목)

### Web Share API

```js
// 이미지 공유
if (navigator.canShare?.({ files: [file] })) {
  await navigator.share({ files: [file], title: `${date} 식단 기록` });
}
```

**폴백 필수:**
- `navigator.share` 미지원 → 이미지 다운로드 버튼 + 텍스트 복사 버튼 노출
- `canShare({files})` 미지원 (일부 구형 iOS) → 텍스트만 `navigator.share`, 이미지는 다운로드
- 클립보드 API 실패 → `<textarea>` 선택 방식 폴백

### 리마인더 알림

- 설정에서 시각 지정 (기본 21:00), ON/OFF
- Notification API + 서비스워커
- 알림 탭 → 앱이 **오늘 탭의 공유 시트가 열린 상태**로 실행
- 이미 공유한 날은 알림 스킵
- iOS는 홈 화면 추가 상태에서만 동작 — **설정 화면에 이 조건을 안내 문구로 표시할 것**

---

## 톤 & 문구 원칙

- **캐주얼한 반말.** 친구한테 말하듯 편하게 (제작자와 친구들이 쓰는 앱이라 반말이 자연스럽다)
- 문장 끝은 `~했어 / ~야 / ~해 봐 / ~돼` 정도. 이모지 남발 금지
- 반말이어도 함부로 굴지 않기 — 친근한 반말이지 명령조나 빈정대는 톤이 아니다
- 부족을 지적할 때 질책하지 않기: "단백질 실패" ❌ → "단백질 30g 남았어" ✅
- 체중 관련 문구에 외모 평가 넣지 않기
- 극단적 목표를 유도하지 않기 — 안전장치가 작동하면 이유를 설명
- 안전장치·상한량 경고처럼 **놓치면 안 되는 문구는 반말이어도 가볍게 쓰지 말 것**
- 앱 하단 상시 고지:
  > 이 앱은 건강 기록 도구야. 의학적 조언이 아니니까 건강 상태나 먹는 약이 있으면 전문가랑 상의해.
