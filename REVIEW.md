# FitLog 인계 노트

> 새 세션에서 코드 리뷰를 받기 위한 정리. 이 문서만 읽어도 무엇을 물어봐야 할지 알 수 있게 썼다.

## 한눈에

- **무엇** — 먹은 음식·보충제·운동을 기록하면 부족한 영양소와 운동을 알려주는 단일 HTML 웹앱
- **제약** — 서버 없음, 라이브러리 없음, 빌드 도구 없음, 데이터는 브라우저 localStorage에만
- **배포** — https://jonijonii.github.io/fitlog/ (GitHub Pages, PWA)
- **사용자** — 제작자 본인 + 비개발자 친구 몇 명
- **규모** — JS 17개 파일 약 5,200줄(테스트 1,417 제외 시 3,800) + CSS 878줄
- **검증** — 단위 395건 / 브라우저(jsdom) 193건 / 서비스워커 20건 + ES5·CSS·배포 정적 검사

## 아키텍처

의존은 위에서 아래로만 흘러야 하는데, **역방향 3개와 순환 1개**가 있다.

```
app.js          부팅 · 라우팅 · 날짜 롤오버 · 서비스워커 등록
views.js        4개 탭 전부 (1,352줄)
onboarding.js   sheet.js   ui.js
─────────────────────────────────────
judge.js        판정 (영양 4단계 · 운동 · 신체)
report.js       요약 텍스트      csv.js   backup.js
supplements.js  templates.js
─────────────────────────────────────
calc.js         BMR/TDEE/목표치
store.js        localStorage 계층
foods.js  nutrition.js  share.js       (의존 없음)
```

**문제가 되는 의존:**

| 방향 | 무엇 | 왜 문제인가 |
|---|---|---|
| `store → calc` | `saveBodyLog`가 `computeTargets` 호출 | 저장 계층이 도메인 계산을 함. 체중 저장이 목표치 재계산을 부르는 게 store 안에 숨어 있다 |
| `ui → calc` | `goalChips`가 목표 규칙을 앎 | UI 컴포넌트가 도메인 지식을 가짐 |
| `supplements → store` | `newId` 하나 때문 | 데이터 모듈이 저장 계층에 묶임 |
| `views ↔ app` | views가 `FitLog.router.render()` 호출 | 순환. 런타임 호출이라 동작은 하지만 구조상 고리 |

## 구조적 약점

정직하게, 심각한 순서로.

**1. `views.js`가 1,352줄** — 오늘·주간·인바디·설정 4개 탭과 30개 함수가 한 파일에.
가장 긴 함수 셋이 100줄을 넘는다(`profileCard` 122, `supplementEditRow` 115, `settings` 112).
탭별 파일 분리가 가장 시급하다.

**2. 전역 재렌더** — 상태가 바뀌면 화면을 통째로 다시 그린다.
단순해서 좋았지만 **스크롤 튐·포커스 손실·날짜 미갱신** 버그가 전부 여기서 나왔다.
지금은 규칙(스크롤 유지, 긴 목록은 부분 갱신, 날짜 확인)으로 막고 있는데,
규칙을 기억해야만 안전한 구조다.

**3. 로드 순서가 곧 의존 그래프** — 모듈 시스템이 없어 `index.html`의 `<script>` 순서가 계약이다.
파일을 추가할 때 순서를 틀리면 조용히 깨진다. 지금은 `build.js`가 순서대로 인라인만 한다.

**4. 칩 그룹 로직이 4개 파일에 중복** — `onboarding.js`, `sheet.js`, `ui.js`, `views.js`.
`ui.goalChips`로 일부만 통합했고 나머지는 각자 구현.

**5. `tests.js`가 단일 함수 1,417줄** — `runTests()` 하나에 395건이 들어 있다.
어디가 깨졌는지 찾기 어렵고, 일부만 돌릴 수 없다.

**6. 스타일이 단일 CSS 878줄** — 컴포넌트별 구획은 있지만 파일은 하나.

## 버그 회고

발생한 순서가 아니라 **실패의 종류**로 묶었다. 같은 종류가 반복됐다.

### 침묵 실패 — 에러 없이 안 되는 것

**온보딩 '시작하기'가 안 눌림**
`store.todayKey()`가 `padStart`(ES2017)를 썼다. 구형 브라우저에서 그 줄만 죽는데,
요약 화면은 그 함수를 안 거쳐서 멀쩡히 떴다. 사용자에겐 "버튼이 안 눌린다"로만 보였다.
→ **ES5 정적 검사**를 테스트에 넣어 `src/js` 전체를 훑게 함.

**서비스워커가 앱을 통째로 막음** *(가장 심각했던 것)*
`cache.addAll`은 전부-아니면-전무다. 파일 하나만 실패해도 통째로 거부되어
**캐시가 텅 빈 채로 워커가 켜졌고**, 그 상태에서 페이지 요청에 빈 504를 주자
사파리가 "인터넷에 연결되어 있지 않습니다"로 막았다.
**앱이 안 열리니 새 워커를 받을 기회도 없어 빠져나올 수 없었다.**
→ `cache.add`를 하나씩 / 페이지엔 절대 빈 응답 금지 / 자가 복구 화면 + `?nosw` 탈출구.

### 표시가 거짓말 — 데이터는 맞는데 화면이 틀림

**온보딩이 안 닫힘**
`.onboarding { display: flex }`가 브라우저 기본 `[hidden]{display:none}`을 이겼다.
JS는 정상이라 에러도 안 났고, jsdom은 우선순위를 다르게 계산해서 **스모크 105건이 전부 통과**했다.
→ 전역 `[hidden]{display:none !important}` + CSS 소스 직접 검사.

**보충제 함량 표시가 실제 값과 다름**
체크리스트가 `preset.amount`(고정 라벨)를 보여줬다. 사용자가 함량을 고쳐도 화면은 그대로.
**계산은 맞는데 표시가 틀린 것은 데이터가 틀린 것보다 나쁘다** — 앱을 못 믿게 된다.
→ 화면 값은 저장된 데이터에서만 뽑기.

### 환경 차이 — 내 쪽에선 안 보이는 것

**자정을 넘겨도 어제 화면**
홈 화면 앱은 껐다 켜지 않으면 계속 살아 있는데, 다시 그리는 코드가 없었다.
→ `visibilitychange`/`focus`/`pageshow`에서 날짜 비교 후 재렌더.

**판정 로직 두 곳이 뒤섞임**
지방 256%가 🟢로, 마그네슘 87%가 🔴로 나왔다.
매크로와 미량영양소는 '과다'의 기준이 다르고(110% vs UL),
부족 판정과 상한 판정은 **서로 다른 값**을 봐야 하는데 하나로 처리했다.
→ 테스트를 통과하고 있었다. **리포트를 눈으로 보다가** 발견.

## 검증 장치

| 종류 | 명령 | 잡는 것 |
|---|---|---|
| 단위 | `node tests/run-tests.js` | 계산·판정·저장 로직 |
| 정적 | 위 명령에 포함 | ES5 문법, `[hidden]` CSS 규칙, 배포 묶음 무결성 |
| 브라우저 | jsdom으로 `dist/index.html` 실제 클릭 | 온보딩~기록~공유 전 과정 |
| 서비스워커 | 가짜 caches/fetch 환경 | 오프라인·설치 실패·복구 |

**이 장치들이 못 잡는 것:**
- 실제 iOS/안드로이드 동작 (공유 시트, 홈 화면 설치, 저장소 정리 정책)
- 보기 좋은지, 쓰기 편한지
- 영양소 값의 정확성 (전부 추정치)
- jsdom과 실제 브라우저가 다르게 동작하는 부분

**테스트 작성 시 함정 셋 (실제로 다 겪음):**
1. jsdom을 믿지 말 것 — CSS 우선순위를 실제와 다르게 계산
2. 함수를 갈아끼우지 말고 환경을 바꿀 것 — `todayKey`만 스텁하면 내부 호출은 여전히 진짜 날짜를 봄
3. 갈아끼울 자리가 필요하면 내보낸 객체를 거치게 할 것

## 새 세션에 물어볼 것

코드를 붙여넣고 이렇게 물어보면 유용할 것 같다.

1. **`views.js` 1,352줄을 어떻게 쪼갤까?** 탭별 파일이 맞나, 아니면 다른 축이 나은가?
2. **역방향 의존 3개를 풀 가치가 있나?** 특히 `store → calc`. 아니면 이 규모에선 과한가?
3. **전역 재렌더를 유지할까?** 규칙 4개로 막고 있는데, 구조를 바꾸는 게 나은가 아니면 이 규모에선 충분한가?
4. **모듈 시스템 없이 로드 순서에 의존하는 게 언제 문제가 되나?** 지금 막을 방법이 있나?
5. **`tests.js` 1,417줄 단일 함수를 어떻게 나눌까?** 브라우저에서도 돌아야 한다는 제약이 있다.
6. **비개발자 사용자용 앱에서 서비스워커가 값어치를 하나?** 이번에 앱을 통째로 막았는데, 오프라인을 포기하는 게 나은 선택인가?
7. **놓치고 있는 실패 유형이 또 있나?** 침묵 실패 / 표시 거짓말 / 환경 차이 — 이 셋 말고.

## 다음에 더 잘하려면

이번에 배운 것을 순서대로.

**먼저 실패 유형을 정하고 방어를 깐다.** ES5 검사도, CSS 검사도, 배포 검사도 전부 **사고가 난 뒤에** 만들었다. 폰에서 도는 앱을 폰 없이 만든다는 걸 처음부터 전제했다면 순서가 반대였을 것이다.

**가드는 일부러 깨뜨려 확인한다.** 아무것도 안 잡는 가드를 통과했다고 착각하기 쉽다. 이번엔 아이콘을 지우고, 캐시 버전을 어긋내고, CSS 규칙을 빼서 실제로 실패하는지 봤다.

**테스트 통과와 정상 동작은 다르다.** 판정 로직 버그 둘은 테스트를 통과한 채로 있었고, 출력을 눈으로 읽다가 찾았다. **결과물을 사람이 읽어보는 단계**가 자동 검증만큼 중요했다.

**스펙을 살아 있는 문서로.** `CLAUDE.md`가 처음 지시서에서 903줄로 자랐다. 배포 후 피드백으로 바뀐 결정(운동 판정 6종→2종, 오늘 기준 알림, 재료 열 제거)과 **그 이유**를 같이 적어두니, 나중에 "왜 이렇게 했지?"가 사라졌다.

**개인 정보는 처음부터 넣지 않는다.** placeholder 예시값, 보충제 프리셋 목록, 스펙 문서의 신체 수치 — 세 번 다 나중에 걷어냈다. 공개 저장소에 올릴 것을 알았다면 처음부터 일반값을 썼을 것이다.
